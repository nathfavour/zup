# Zup Skills Catalog

**Read this file first.** Then open only the skill matching your task.

## Skills

| Skill | Helps with |
|-------|------------|
| `openbricks-4.0` | Core OpenBricks 4.0 design system: pitch black components (`#000000`), deep ash containers (`#161412`), pure white text, and crisp borders. |
| `zup-nostr` | Real on-chain Nostr metadata (Kind 0), notes (Kind 1), reactions (Kind 7), follow lists (Kind 3), relays & connection pools. |
| `zup-quality` | Zup Quality Shield: multi-layer wire drop, Web of Trust scoring, SimHash near-duplicate detection, and Shannon entropy heuristics. |
| `zup-oauth` | Sign in with Kylrix: RFC 7636 PKCE flow, `/oauth/consent`, `/api/v1/oauth/token`, `/api/v1/me`, and NIP-78 binding. |
| `zup-vault` | Zero-knowledge MEK, Argon2id KEK, WebAuthn passkeys, local-first RxDB encryption, and in-memory key lifecycle. |
